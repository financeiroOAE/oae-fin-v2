name: Apply Round 3 Financial UI 2026-08-25

on:
  push:
    branches:
      - fix-dre-projects-ui-2026-08-25
    paths:
      - scripts/apply-round3-financial-ui-2026-08-25.py
      - .github/workflows/apply-round3-financial-ui-2026-08-25.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix-dre-projects-ui-2026-08-25
          fetch-depth: 0
      - name: Aplicar correcoes
        run: python3 scripts/apply-round3-financial-ui-2026-08-25.py
      - name: Commitar alteracoes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src
          if git diff --cached --quiet; then
            echo "Nenhuma alteracao nova."
            exit 0
          fi
          git commit -m "fix: DRE projetos fluxo filtros e auditoria"
          git push origin HEAD:fix-dre-projects-ui-2026-08-25
